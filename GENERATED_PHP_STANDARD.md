# Generated PHP Standard

PhpTranspilerが生成するPHPは、PHP 8.4以降の安全・安定サブセット向けAdlaire生成PHP規則に従う。PHP 8.5以降で非推奨・廃止・高リスクとなる構文/APIは生成しない。

## 基本規則

- 先頭は必ず `<?php` とする。
- 2行目に `Generated by PhpTranspiler` ヘッダーを置く。
- PHP閉じタグ `?>` は出力しない。
- 改行はLFのみとする。
- タブ、末尾空白、余分な先頭空白を出力しない。
- 同一入力と同一PhpTranspilerバージョンからは、常に同一バイト列を生成する。

## 安全規則

- 未変換TypeScript断片を出力しない。
- `eval`、`assert`、`create_function`、shell実行系、process実行系を出力しない。
- 可変変数、変数関数呼び出しを出力しない。
- 動的 `include` / `require` を出力しない。
- TypeScript import由来の `require_once __DIR__ . '/...';` のみを許可する。
- ServerFoundation移行で必要なPHP標準機能は、明示的な `Php.*` helper経由でのみ生成する。
- 文字列リテラル内のCSS selector、正規表現、URL、表示文言は、識別子変換、プロパティアクセス変換、static参照変換の対象にしない。
- 入れ子関数呼び出しを含む `new Class(...)` の引数、static呼び出し結果の文字列メソッド、裸識別子引数を未変換のまま出力しない。

## データベース生成規則

- PHP生成物の既定データベースはSQLiteとする。
- SQLiteアクセスは `PDO` の `sqlite:` DSN、または `SQLite3` / `SQLite3Stmt` / `SQLite3Result` の明示 `Php.*` helper経由でのみ生成する。
- 旧 `sqlite_*` 関数、およびPHP 8.5以降で非推奨となるPDOドライバ固有旧 `PDO::*` 定数は生成しない。
- PDOの属性・fetch mode・parameter typeは、`PDO::ATTR_*`、`PDO::ERRMODE_*`、`PDO::FETCH_*`、`PDO::PARAM_*` の汎用定数のみを使用する。

## PHP 8.4生成規則

- PHP 8.4で追加された標準APIは、明示許可した `Php.*` helperからのみ生成する。
- `BcMath\Number`、`bcceil` / `bcfloor` / `bcround` / `bcdivmod`、`fpow`、`array_all` / `array_any` / `array_find` / `array_find_key`、`grapheme_str_split`、`request_parse_body`、HTTP最終レスポンスヘッダー関数、DateTime microsecond/timestamp系、DBA/ODBC/DOM/Intl/PCNTL/PGSQL/SPL/SOAP/Tidy/XMLReader/XMLWriter/XSL追加API、DOM CSS selector/classList操作、Deprecated/RequestParseBodyException/StreamBucket、Reflection追加メソッド、Reflection lazy object API、ReflectionProperty hook API、PropertyHookType、OpenSSL鍵/署名/検証、Argon2 provider定数、cURL追加情報/timeout定数、LDAP TLS 1.3定数、IntlChar/IntlDateFormatter/NumberFormatter追加定数、Sodium AEGIS定数/keygen/encrypt/decrypt、XSLTProcessor最大テンプレート深度/変数プロパティ、`PDO::connect()`、`Pdo\Sqlite` / `Pdo\Mysql` / `Pdo\Pgsql` / `Pdo\Odbc` / `Pdo\DbLib` / `Pdo\Firebird`、`Pcntl\QosClass`、`RoundingMode`、`Dom\HTMLDocument` / `Dom\XMLDocument`、`Deprecated`、`RequestParseBodyException`、`Dba\Connection`、`Odbc\Connection` / `Odbc\Result`、`Soap\Url` / `Soap\Sdl`、`StreamBucket`、`ReflectionConstant`生成/参照、`ZipArchive::ER_TRUNCATED_ZIP`、PHP 8.4追加定数は生成対象とする。
- 暗黙nullableパラメータ、`class _`、`trigger_error(..., E_USER_ERROR)`、非推奨mysqli関数/メソッド/定数、PHP 8.4で非推奨化された標準/拡張定数と関数呼び出しは生成しない。
- PHP 8.4のproperty hookとasymmetric property visibilityは、TypeScript移植表現が確定するまで生成しない。

## PHP 8.5前方互換規則

- 非正規cast名 `(boolean)`、`(integer)`、`(double)`、`(binary)` は出力しない。
- `case ...;` 形式、backtick operator、`__sleep()` / `__wakeup()`、`$http_response_header` は出力しない。
- `curl_close()`、`curl_share_close()`、`finfo_close()`、`imagedestroy()`、`xml_parser_free()`、`socket_set_timeout()` は出力しない。
- `DATE_RFC7231`、`E_STRICT`、`MHASH_*`、PDOドライバ固有の旧 `PDO::*` 定数は出力しない。
- `array_key_exists(null, ...)` と `trigger_error(..., E_USER_ERROR)` は出力しない。
- PHP標準ライブラリは明示許可した `Php.*` helper経由でのみ生成する。
